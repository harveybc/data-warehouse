{
  "store_id": "olap_cube",
  "title": "predictor OLAP cube",
  "description": "SELECT for query; append-only gov_* through the governed routes; never reset",
  "kind": "warehouse",
  "engine": "sql_olap",
  "transport": "http",
  "web_host": "127.0.0.1",
  "web_port": 5067,
  "backend": {
    "entry_point": "predictor_olap",
    "distribution": "predictor-olap-store",
    "settings": {
      "schema": "public",
      "holdout_start": "2025-01-01",
      "lake_id": "olap_cube",
      "sqlite_path": null
    }
  }
}
